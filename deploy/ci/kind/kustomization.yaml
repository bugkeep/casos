apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: casos-ci

resources:
  - namespace.yaml
  - mysql.yaml
  - casos.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: casos-app-config
    files:
      - app.conf=app.conf
